<span class="badge @BootstrapClass status-badge status-badge--@VariantClass">@ChildContent</span>

@code {
    [Parameter] public StatusBadgeVariant Variant { get; set; } = StatusBadgeVariant.Secondary;
    [Parameter] public RenderFragment? ChildContent { get; set; }

    private string VariantClass => Variant.ToString().ToLowerInvariant();

    // Bootstrap bg-* class is kept alongside the soft-token styling below purely so existing
    // E2E locators (e.g. ".badge.bg-success") keep matching without needing to be rewritten.
    private string BootstrapClass => Variant switch
    {
        StatusBadgeVariant.Success => "bg-success",
        StatusBadgeVariant.Warning => "bg-warning",
        StatusBadgeVariant.Danger => "bg-danger",
        StatusBadgeVariant.Info => "bg-info",
        StatusBadgeVariant.Primary => "bg-primary",
        _ => "bg-secondary",
    };
}
