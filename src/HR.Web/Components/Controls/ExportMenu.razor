@using Syncfusion.Blazor.SplitButtons

<SfDropDownButton Content="Export" IconCss="fa-solid fa-download" CssClass="e-flat hr-toolbar-export-btn" Items="_items">
    <DropDownButtonEvents ItemSelected="HandleItemSelected" />
</SfDropDownButton>

@code {
    [Parameter] public EventCallback<string> OnItemSelected { get; set; }

    private static readonly List<DropDownMenuItem> _items =
    [
        new() { Id = "hr-excel", Text = "Excel", IconCss = "fa-solid fa-file-excel" },
        new() { Id = "hr-csv",   Text = "CSV",   IconCss = "fa-solid fa-file-csv" },
        new() { Id = "hr-pdf",   Text = "PDF",   IconCss = "fa-solid fa-file-pdf" },
    ];

    private async Task HandleItemSelected(MenuEventArgs args)
    {
        if (args.Item?.Id is { } id)
            await OnItemSelected.InvokeAsync(id);
    }
}
